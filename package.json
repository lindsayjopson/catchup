{
  "name": "catchup",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && next export -o docs"
  },
  "dependencies": {
    "@zeit/next-css": "^0.1.4",
    "next": "^6.0.4-canary",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  }
}
